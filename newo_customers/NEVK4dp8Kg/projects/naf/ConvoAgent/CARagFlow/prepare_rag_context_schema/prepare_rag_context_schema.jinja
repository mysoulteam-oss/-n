{% set schema =
{
  "type": "object",
  "properties": {
    "search_result": {
      "type": "object",
      "properties": {
        "article_id_1": {
          "type": "string",
          "description": "Closest available article id from the knowledge base for the latest meaningful conversation topic. Always return the single best available article id, even if it is broad or indirectly related. Use \"none\" only if every article is unrelated."
        },
        "article_id_2": {
          "type": "string",
          "description": "Second best distinct supporting article id for the same topic, or \"none\" if no additional useful article remains."
        },
        "article_id_3": {
          "type": "string",
          "description": "Third best distinct supporting article id for the same topic, or \"none\" if no additional useful article remains."
        }
      },
      "required": ["article_id_1", "article_id_2", "article_id_3"]
    }
  },
  "required": ["search_result"]
}
%}

{{Return(val=json.dumps(schema, ensure_ascii=False, indent=2))}}
