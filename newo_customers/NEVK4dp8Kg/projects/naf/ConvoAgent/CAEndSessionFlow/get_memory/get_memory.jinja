{{Return(val=memory_utils.get_memory(
    actors=actors,
    count=count,
    from_date=from_date,
    include_system=include_system,
    include_thoughts=include_thoughts,
    to_date=to_date,
    user_id=user_id
))}}
